<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="hrd-room-service"/>
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name" defaultValue="logs/hrd-room-service.log"/>
    <springProperty scope="context" name="ROLLING_FILE_NAME_PATTERN"
                    source="logging.logback.rollingpolicy.file-name-pattern"
                    defaultValue="logs/archived/hrd-room-service-%d{yyyy-MM-dd}.%i.log.gz"/>
    <springProperty scope="context" name="ROLLING_MAX_FILE_SIZE"
                    source="logging.logback.rollingpolicy.max-file-size" defaultValue="10MB"/>
    <springProperty scope="context" name="ROLLING_MAX_HISTORY"
                    source="logging.logback.rollingpolicy.max-history" defaultValue="30"/>
    <springProperty scope="context" name="ROLLING_TOTAL_SIZE_CAP"
                    source="logging.logback.rollingpolicy.total-size-cap" defaultValue="1GB"/>

    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{requestId:-}] [%X{userId:-}] [%X{clientIp:-}] %X{method:-} %X{path:-} %logger{36} - %msg%n"/>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${ROLLING_FILE_NAME_PATTERN}</fileNamePattern>
            <maxFileSize>${ROLLING_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${ROLLING_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${ROLLING_TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE_PRETTY" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app":"${APP_NAME}"}</customFields>
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>clientIp</includeMdcKeyName>
            <includeMdcKeyName>method</includeMdcKeyName>
            <includeMdcKeyName>path</includeMdcKeyName>
        </encoder>
    </appender>

    <springProfile name="json">
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <springProfile name="!json">
        <root level="INFO">
            <appender-ref ref="CONSOLE_PRETTY"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>
